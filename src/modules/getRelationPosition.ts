import type { IRelation } from "../interfaces/data-model";
import type { IRelationOnDiagram } from "../interfaces/diagram";

export const getRelativePosition = (
  relations: IRelation,
  position: IRelationOnDiagram
): IRelation[] => {
  return [];
};
